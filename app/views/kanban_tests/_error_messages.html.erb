<% if @kanban_test.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      The form contains <%= pluralize(@kanban_test.errors.count, "error") %>.
    </div>
    <ul>
    <% @kanban_test.errors.full_messages.each do |msg| %>
      <li>* <%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
